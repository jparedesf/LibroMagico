Programa en Visual Studio para comprobar el funcionamiento del envio y recepcion de informacion del arduino al módulo por puerto serial.
Para el correcto funcionamiento es necesario tener conectado el arduino con el lector RFID por el puerto serial. COM4
